import { Sqlite } from '../../../icons/server';
import { ICON_RESOURCE_PATH } from '../../icon';
import React from 'react';
import { INodeIcon } from '../server-icon.config';

export const SqliteIconSetting:INodeIcon = {
  icon: <Sqlite />,
  iconPath: ICON_RESOURCE_PATH.Sqlite,
};
