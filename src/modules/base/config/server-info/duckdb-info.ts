import { IServerPreference } from '../server-info.config';

export const DuckDBPreference: IServerPreference = {
  name: 'DuckDB',
  displayName: 'DuckDB',
  isSupport: false,
  nextSupport: false,
};
