import { IServerPreference } from '../server-info.config';

export const HBasePreference: IServerPreference = {
  name: 'HBase',
  displayName: 'Apache HBase',
  isSupport: false,
  nextSupport: false,
};
