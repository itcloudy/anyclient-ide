```cmd
git clone https://github.com/microsoft/monaco-editor
yarn # 根目录安装依赖
yarn build-monaco-editor # 构建本地的monaco-editro

cd website # 进入website目录
yarn  # 安装依赖
yarn typedoc # 编译本地的TS文档
yarn dev  # 启动服务

```
